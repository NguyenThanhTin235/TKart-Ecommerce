spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tkartdb
    username: root
    password: 23092005
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8080
jwt:
  secret: your_jwt_secret
  expiration: 86400000
