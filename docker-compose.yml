version: "3.8"
services:
  db:
    image: mongo:6.0
    container_name: tkart-mongodb
    restart: always
    environment:
      MONGO_INITDB_DATABASE: tkartdb
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    volumes:
      - db_data:/data/db
volumes:
  db_data:
